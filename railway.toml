[build]
builder = "dockerfile"
dockerfilePath = "docker/Dockerfile.backend"

[deploy]
startCommand = "sh scripts/start_backend.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
